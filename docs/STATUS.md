# STATUS — Site de Portfólio devdapraia

**Última atualização:** 2026-09-10 (14ª atualização do dia — **Fase 6 construída** no branch `rascunho`, aguardando conferência visual do autor)
**Fase atual:** Fase 6 — Bloco de processo de design (Figma) — **construída no `rascunho`** (ainda não publicada). As Fases 2, 3, 4 e 5 + o ajuste de responsividade do mobile continuam sendo o que está no `main` (merge `d19b106`). Próxima fase planejada: **Fase 7 — Seção de tecnologias e linha de formação**. (O site só fica no ar de fato quando o GitHub Pages a partir de `main` + HTTPS for ligado no painel — pendência herdada da Fase 1, depende do autor.)

**Ajuste fora de fase (autor, 2026-09-10) — parte da Fase 10 antecipada:** a pedido do autor, com base em prints do mobile, entrou um bloco de **media queries** (`768px` e `480px`) no fim de `css/estilo.css` — o **primeiro CSS responsivo do projeto** (a base não tinha nenhum). Cobre: hero empilhado no celular (foto acima do texto, centralizado, com respiro; avatar `130px`/`110px`; `h1` `2.2em`/`1.9em`), menu sem encostar nas bordas (`gap` menor, fonte `0.8em`, `flex-wrap`, recuo lateral `24px`/`16px`), `.sobre-grid` e `.projeto-imagens` passando a uma coluna, e paddings laterais de seções/divisor/rodapé na mesma escala do hero. **Só media queries — o desktop fica idêntico** (hero lado a lado, design aprovado). **Não fecha a Fase 10:** a varredura completa a partir de 360px, o restante da tabela de 12.8, o `scroll-margin-top` do menu e a revelação por rolagem continuam sendo trabalho da Fase 10. A próxima fase planejada continua sendo a **Fase 6**.
**Próximo passo recomendado:** o autor confere no Live Server o branch `rascunho` nos dois temas e, se possível, no celular — atenção nesta fase às **3 imagens do bloco de processo em tamanho real** (segunda conferência de segurança — FSD 21.4/3): `app-grade-de-quartos`, `app-detalhe-do-quarto` e `app-conferencia-de-limpeza`. **Ponto a confirmar:** são recortes de protótipo do app (em construção) feitos no Figma; os nomes que aparecem nas telas ("João Silva", "Maria Silva", "João da Silva") são genéricos de teste, não hóspedes reais — a IA tratou como "dados fictícios" na mesma leitura da Fase 5, mas a palavra final é do autor. Conferir também que a **ampliação ao toque** abre e fecha nos três recortes (clique no fundo, botão ×, tecla `Esc`) e que a seção fica logo após o card do HFS, antes de Tecnologias. Estando OK, o fluxo de publicação (push do `rascunho`, merge em `main`, push do `main`, retorno ao `rascunho`) é executado. Depois começa a **Fase 7 — Seção de tecnologias e linha de formação**. Ver `docs/PLANO.md`.

**Decisão consciente registrada nesta fase (autor, 2026-09-10):** os valores monetários visíveis nas imagens do HFS (`R$ 1.895,00`, `R$ 2.060,56`, a tabela de lançamentos) **são dados fictícios / de teste**, confirmado pelo autor no chat. Publicados **como estão**, sem tarja. Atende o critério 26.4 ("as imagens do HFS não exibem valores financeiros reais"); é uma leitura mais frouxa da palavra "mascarados" de FSD 21.1/RN02, adotada porque não há valor real a proteger. As 4 capturas brutas (`hgs-*.png` / `hfs-*.png`, na raiz) **não entram no repositório** — foram para o `.gitignore` junto com `figma-*.png` e os vídeos; só a versão tratada (recortada + WebP) em `imagens/hgs|hfs/` é versionada.

**Alterações conscientes de escopo (2026-09-10, decisão do autor) — resumo:** (1) `#sobre` vem **antes de `#projetos`** e o menu passa a `Sobre · Projetos · Tecnologias · Contato` — contraria o FSD (1, 5.3, 12.1, 13, 25), atualizado; motivo: dar contexto a quem chega sem repertório. (2) Posicionamento cobre **marketing e desenvolvimento** — Marketing (UNIBRA, 2020) ao lado de ADS; "objetivo" serve às duas frentes — contraria o FSD (6/A1, 6/D1, 10, 26.5), atualizado. (3) Bloco `.apresentacao` (Fase 2) **removido** — marcação e CSS — e o "quem sou" **unificado** no card "quem sou" da seção Sobre; contraria o FSD 6/A3 (módulo descontinuado), 5.3, 12.7, 25 item 10, 26.5 — atualizado. (4) **Botão "Pedir a gravação" nos dois cards.** Existem gravações de tela do HGS e do HFS funcionando; elas **não entram no site** (teto de peso 1,5 MB — FSD 21.2; vídeo fora de escopo — FSD 7.1) e os `apresentacao-*.mp4` ficam fora do repositório (`.gitignore`). O botão de contato dos cards, antes "Solicitar apresentação do sistema" só no HGS, passou a **"Pedir a gravação"** nos **dois** cards, `.btn-ghost`, cada um com mensagem de WhatsApp que identifica o sistema; acima dele, a linha `.projeto-gravacao` sobre o envio pessoal (sem prazo). Contraria o FSD 6/B3, 6/C1, 12.3, 13/Fluxo 3, 21.1, 26.4 — atualizado; RN18 e RN13 preservadas. Ver as entradas no histórico. Nenhuma abre ou fecha fase.

**Pendência da Fase 3 resolvida na Fase 5:** o placeholder textual do elemento 7 ("Imagens do sistema em funcionamento — em preparação") **foi substituído** pelas quatro capturas reais, conferidas e comprimidas (duas por card). Os cards de projeto agora vão ao ar em estado pleno (FSD 12.3).

**Ressalva da Fase 5 (não bloqueia a conclusão):** ~~a grade de imagens do card usa duas colunas e ainda não empilha em telas estreitas~~ **Resolvido no ajuste fora de fase de 2026-09-10:** abaixo de `768px` a grade de imagens (`.projeto-imagens`) passa a **uma coluna**. As imagens seguem legíveis e amplificáveis ao toque nos dois casos.

**Pendência conhecida herdada da Fase 1:** o **GitHub Pages a partir de `main` + HTTPS** em `devdapraia.github.io` depende de configuração no painel do GitHub (não é possível por código). Enquanto não confirmado, o site não está publicado — a conferência é feita localmente pelo Live Server.

**Ressalva da Fase 2 (não bloqueia a conclusão):** ~~o hero herdado da base não empilha em telas estreitas~~ **Endereçado no ajuste fora de fase de 2026-09-10:** abaixo de `768px` o hero empilha (foto acima do texto, centralizado, com respiro), `h1` e avatar reduzem em `768px` e `480px`, e os paddings laterais caem para `24px`/`16px`. A conferência final "sem rolagem no celular a partir de 360px" (critérios 26.3/26.7) ainda é fechada na **Fase 10**, junto com o resto da tabela de 12.8. No desktop o hero está correto (inalterado).

**Controle de versão:** repositório Git inicializado; primeiro commit `c311b26` com a base intacta (ponto de retorno); remote `origin` = `https://github.com/devdapraia/devdapraia.github.io.git`. A Fase 1 foi para o `main` no merge `5a0c80f`. Em 2026-09-10 o autor conferiu e liberou (**"conferi, pode publicar"**): `rascunho` → `main` pelo merge `d19b106` (`git merge --no-ff`), `git push origin main` feito, branch de volta em `rascunho`. Com isso as **Fases 2, 3, 4 e 5 + o ajuste de responsividade do mobile** ficaram publicadas no `main`. `origin/main` e `origin/rascunho` sincronizados. `.gitignore` revisado e `.gitattributes` criado para a stack estática. Nenhum segredo, credencial ou dado de terceiro versionado.

---

## Estado atual do projeto

Terreno preparado, **projeto versionado em Git e com backup no GitHub**, **Fase 1 concluída** (CSS/JS extraídos, navegação por âncora, base intacta no `main`), **Fase 2 concluída** (bloco de abertura: foto real, posicionamento, selo, botões; a apresentação curta feita nesta fase foi **removida depois** — ver alterações conscientes de escopo), **Fase 3 construída** no branch `rascunho` (a seção `#projetos` deixou de ser uma linha do tempo de versões técnicas e passou a apresentar **dois cards de projeto** — HGS e HFS — na estrutura fixa de oito elementos, idêntica entre os dois; foi criada a **pílula de tecnologia** sem ícone/barra/nível, reaproveitável na Fase 7; o card do HGS tem a nota "protótipo original em Java; app em construção com Flutter e Dart" e o botão "Solicitar apresentação do sistema" — `.btn-ghost`, WhatsApp) e **Fase 4 construída** no branch `rascunho`: a autoavaliação de nível de habilidade (`.stack-item` / `.tech-dot` com os rótulos "avançado", "intermediário"…) saiu da marcação da seção `#tecnologias` e do CSS. A seção `#tecnologias` ficou com o rótulo, o `h2` e uma linha neutra provisória ("Conteúdo em preparação.") até a Fase 7 trazer os dois grupos por contexto de uso e a linha de formação. A seção `#sobre` ainda tem os cards da base (a redação final em três parágrafos é da Fase 8), mas **já foi reposicionada** para **antes de `#projetos`** — é a **primeira seção depois do hero**, porque o bloco `.apresentacao` (criado na Fase 2) **foi removido** (marcação e CSS) e seu "quem sou" foi **unificado** no card "quem sou" (que agora abre com a trajetória completa: marketing/atendimento/coordenação → recepção → ADS → constrói software). O **menu foi reordenado** para `Sobre · Projetos · Tecnologias · Contato`, e os cards "formação" e "objetivo" refletem o novo posicionamento: **Marketing (UNIBRA, 2020) ao lado de ADS** e um objetivo que serve a **desenvolvimento ou marketing**. O `h1`/linha do hero seguem sem mencionar marketing (servem às duas leituras); a avaliação do hero fica registrada como item da Fase 8. **Fase 5 construída** no branch `rascunho`: o elemento 7 dos dois cards deixou de ser um placeholder textual e passou a exibir **duas imagens reais por card** (HGS: grade de quartos, central de alertas; HFS: contas vencidas, lançamentos), recortadas para tirar a barra de status do celular e a barra do navegador, convertidas para WebP dentro do teto de 150 KB, com `width`/`height`/`loading="lazy"`/`alt` descritivo sem dado de hóspede nem nome da pousada. Cada imagem abre em **ampliação ao toque** por sobreposição própria em `js/script.js` (sem biblioteca), com o link direto ao arquivo funcionando mesmo sem JavaScript. O site continua **não publicado** pelo GitHub Pages (pendência herdada da Fase 1, depende do autor).

### O que foi feito na Fase 6 (branch `rascunho`)

- **Três recortes do Figma tratados** — `figma-1/2/3.png` (1170×2532, entregues soltas na raiz, ignoradas pelo Git por `/figma-*.png`) foram **conferidas em tamanho real** contra dado de terceiro (FSD 21.4). São **mockups de protótipo do app do HGS em construção**, montados no Figma. O que aparece: grade de quartos por andar com status, detalhe de um quarto com ações, checklist de conferência de limpeza. Nomes nas telas — "João Silva", "Maria Silva", "João da Silva" — são **genéricos de teste** (equivalentes a "Fulano"); não há documento, telefone, e-mail, valor financeiro real, nome da pousada, nome de funcionário nem credencial. Não há barra de navegador (é mockup de app). Recorte de **150px no topo** removeu a barra de status do simulador (hora 09:41, sinal, wi‑fi, bateria) e o recorte do notch. Conversão para WebP q82 com Pillow (**não é dependência do site**): `imagens/processo/app-grade-de-quartos.webp` (75,6 KB), `imagens/processo/app-detalhe-do-quarto.webp` (79,2 KB), `imagens/processo/app-conferencia-de-limpeza.webp` (77,8 KB) — todas 1170×2382, << 150 KB. **Decisão registrada:** as telas são tratadas como "dados fictícios" na mesma leitura adotada na Fase 5 para os valores do HFS; a confirmação final é do autor na conferência visual.
- **`imagens/processo/.gitkeep` removido** (a pasta agora tem arquivos reais). As três PNGs brutas seguem fora do versionamento (`git check-ignore` confirma).
- **Marcação (`index.html`)** — nova `<section class="section" id="processo">` **imediatamente após o card do HFS** e antes do `<hr>` que separa de `#tecnologias`. Conteúdo: `p.section-label` "processo de design" + `h2` "Protótipo e design system do app" + `p.processo-intro` (uma linha de contexto: o app está em construção e passou por protótipo navegável e design system no Figma) + `div.processo-imagens` com três `<a class="projeto-imagem" href="…" target="_blank" rel="noopener">` envolvendo `<img width="1170" height="2382" loading="lazy" alt="…">`. Os `alt` descrevem cada tela sem citar hóspede nem a pousada. O bloco é delimitado por comentários `FASE 6` e **inclui o `<hr class="divider">` que o precede** — removê-lo inteiro volta ao estado da Fase 5 sem tocar em nenhuma outra seção. Contagem de dividers passou de 2 para **3** (um por transição de seção: sobre|projetos, projetos|processo, processo|tecnologias).
- **CSS aditivo (bloco "FASE 6" em `css/estilo.css`, entre o bloco "FASE 5" e o "AJUSTE DE RESPONSIVIDADE")** — `.processo-intro` (0.9em, peso 300, `--texto-muted`, margem inferior 24px da escala) e `.processo-imagens` (`display: grid`, `repeat(3, 1fr)`, `gap: 16px`). Duas media queries **próprias do bloco** para ele seguir autocontido: em `≤768px` os recortes passam a duas colunas, em `≤480px` a uma. As imagens reaproveitam `.projeto-imagem` / `.projeto-imagem img` / `.img-overlay` da Fase 5 (borda 1px `--card-border`, raio 12px, hover `--destaque`, ampliação ao toque). Nenhuma regra herdada reescrita; nada removido; chaves balanceadas (97/97); `@media` no arquivo: 4.
- **`js/script.js` não foi tocado** — o bloco "FASE 5" já seleciona `.projeto-imagem`, então os três recortes novos ganham a ampliação ao toque sem uma linha de JavaScript. Sem JavaScript, cada `<a>` ainda abre a imagem em nova aba. O `IntersectionObserver` herdado (lista `.timeline-item` vazia) continua intocado — decisão da Fase 10.
- **Testes locais** (`python -m http.server`): `/`, `css/estilo.css`, `js/script.js`, `favicon.ico`, `imagens/avatar.webp`, os `.webp` de `hgs`/`hfs`/`processo`, currículo em PDF → **200**; `node --check js/script.js` OK; sem `style=`/`onclick=`/caminho absoluto na marcação; `.btn-primary` **1×** (hero), `.btn-ghost` **3×** (hero + 2 cards) — inalterado; `.projeto-imagem` **7×** (4 dos cards + 3 do processo); **peso total dos assets servidos 557,1 KB** (teto 1,5 MB); maior imagem 86,7 KB (teto 150 KB). Sem erros registrados. **Ressalva:** a varredura de responsividade a partir de 360px (inclusive os três recortes lado a lado) é fechada na Fase 10. Aguardando conferência visual do autor no Live Server / celular; depois: push do `rascunho`, merge em `main`, push do `main`, retorno ao `rascunho`. **Próxima fase: Fase 7 — Seção de tecnologias e linha de formação.**

### O que foi feito na Fase 5 (branch `rascunho`)

- **Quatro capturas tratadas** — as capturas brutas `hgs-1/2.png` e `hfs-1/2.png` (1170×2532, entregues soltas na raiz) foram **conferidas em tamanho real** contra dado de terceiro (FSD 21.4): só há números de quarto, status e alertas no HGS, e valores fictícios/de teste no HFS (confirmado pelo autor). Nenhum nome de hóspede, documento, telefone, nome de pousada ou credencial. Recorte de `150px` no topo (barra de status iOS) e `232px` na base (barra flutuante do Safari + indicador de home) — a URL `hfs.web.br.com` que aparecia na barra do navegador saiu no recorte. Conversão para WebP (qualidade 82, largura mantida em 1170px, abaixo do teto de 1200px). Resultado: `imagens/hgs/grade-de-quartos.webp` (57,2 KB), `imagens/hgs/central-de-alertas.webp` (86,7 KB), `imagens/hfs/contas-vencidas.webp` (55,8 KB), `imagens/hfs/lancamentos.webp` (68,6 KB) — todas << 150 KB. Ferramenta usada só no tratamento: Pillow (Python) — **não é dependência do site**.
- **Capturas brutas fora do repositório** — `.gitignore` recebeu `/hgs-*.png` e `/hfs-*.png` (mesma linha de `figma-*.png` e dos vídeos). `git check-ignore` confirma que as 4 ficam fora do versionamento; só a versão tratada em `imagens/hgs|hfs/` é rastreada. `imagens/hgs/.gitkeep` e `imagens/hfs/.gitkeep` removidos (as pastas agora têm arquivo real).
- **Marcação (`index.html`)** — o `<div class="projeto-imagens">` de cada card trocou o `<p class="projeto-imagens-placeholder">` por dois `<a class="projeto-imagem" href="<arquivo>" target="_blank" rel="noopener">` envolvendo um `<img>` com `width="1170" height="2150"`, `loading="lazy"` (as duas imagens ficam abaixo da dobra) e `alt` descritivo. O `rel="noopener"` (sem `noreferrer`) segue o texto literal de FSD 12.4 — os links apontam para arquivo do próprio site.
- **CSS aditivo (bloco "FASE 5" no fim de `css/estilo.css`)** — `.projeto-imagens` ganha `display: grid` de duas colunas com `gap: 16px`; `.projeto-imagem`/`.projeto-imagem img` com borda 1px `--card-border` e raio `12px` (hover: borda `--destaque`); a sobreposição `.img-overlay` (fundo `rgba(10,7,18,0.92)` — escuro nos dois temas de propósito, para a captura ter fundo neutro; mesma constante que a base usa em `--nav-bg`), `.img-overlay img` (`max-width: 100%`, `max-height: 90vh`), `.img-overlay-fechar` (botão redondo de 44px, mesmo padrão visual do `.theme-toggle`, texto `#0a0712` sobre `--destaque` — exceção documentada no DESIGN) e `body.overlay-aberto { overflow: hidden }` para travar a rolagem. Consome variáveis de tema; só transição de opacidade (0.2s). **Única remoção:** o `.projeto-imagens-placeholder` (andaime temporário que a própria Fase 3 tinha adicionado, agora substituído — critério 26.2); nenhuma regra herdada da base foi reescrita. Chaves balanceadas (68/68).
- **JavaScript aditivo (`js/script.js`)** — bloco "FASE 5" em IIFE: se não houver `.projeto-imagem` na página, não faz nada. No clique, `preventDefault()` e abre a sobreposição criada por `document.createElement` (sem `innerHTML`); fecha por clique no fundo, no botão `×` e com `Esc`; adiciona/remove `overlay-aberto` no `<body>`; remove o nó depois da transição. O `IntersectionObserver` herdado (hoje apontando para `.timeline-item`, lista vazia) **não foi tocado** — decisão da Fase 10. Sem `console.log`, sem `alert`. `node --check` passou.

### O que foi feito na Fase 4 (branch `rascunho`)

- **Autoavaliação de nível removida** — a marcação `.stack-grid` / `.stack-item` / `.tech-dot` (com os spans `tech-name` e `tech-level` "avançado", "intermediário", "básico", "iniciando", "em breve") saiu da seção `#tecnologias` de `index.html`. O bloco CSS `STACK` inteiro (`.stack-grid`, `.stack-item`, `.stack-item:hover`, `.stack-item .tech-name`, `.stack-item .tech-level`, `.tech-dot`, `.dot-on`, `.dot-off`) saiu de `css/estilo.css`. `grep` em `index.html` + `css/estilo.css` + `js/script.js` confirma que **nada mais usa** essas classes (as ocorrências restantes estão só em comentários que documentam a remoção). **Não há mais nível de domínio de tecnologia declarado em nenhum ponto do site — nem por texto, nem por barra, nem por bolinha** (RN16, FSD 12.11, critério 26.5).
- **Seção `#tecnologias` publicável e íntegra** — ficou com `p.section-label` "tecnologias" + `h2` "Stack atual" (provisório) + `p.secao-preparo` "Conteúdo em preparação.". Os dois grupos por contexto de uso ("Usei em projetos que estão no ar" / "Estudando agora") e a linha de formação (ADS, conclusão prevista para junho de 2028) entram na **Fase 7**, reaproveitando a pílula de tecnologia criada na Fase 3.
- **Pílula de tecnologia cobre o uso restante** — `.tech-lista` / `.tech-pill` (Fase 3) é o único componente de tecnologia vivo; será reusado na Fase 7. Nenhum resquício de `.stack-*` a reaproveitar.
- **CSS aditivo** — bloco "FASE 4" no fim de `css/estilo.css` só com `.secao-preparo` (texto corrido, peso 300, `--texto-muted`), consumindo variável de tema, funcionando nos dois temas. Única remoção de CSS: o bloco `STACK` (remoção autorizada — FSD 12.11). Nenhuma regra herdada foi reescrita. Chaves balanceadas (59/59).
- **`js/script.js` não foi tocado** — a alternância de tema (`.theme-toggle` + `addEventListener`) e o `IntersectionObserver` continuam como estavam; a decisão sobre o observer (hoje apontando para uma lista vazia) é da Fase 10.
- **`.btn-primary` 1× (hero) e `.btn-ghost` 2× (hero + botão do HGS)** — inalterado.

### O que foi feito na Fase 3 (branch `rascunho`)

- **Linha do tempo removida** — `.timeline` / `.timeline-item` saíram da marcação (`index.html`, seção `#projetos`) e do CSS (`css/estilo.css`, bloco `TIMELINE` inteiro). Confirmado por `grep` que nada mais os usa em arquivos vivos. **Não há mais linha do tempo de versões técnicas em nenhum ponto da página** (RN, 26.4, 7.2). O `IntersectionObserver` de `js/script.js` que apontava para `.timeline-item` **foi mantido intacto** — a decisão de reapontar o seletor ou remover a revelação por rolagem é da **Fase 10** (`docs/PLANO.md`); hoje ele apenas observa uma lista vazia, sem efeito.
- **Componente de card de projeto** (`css/estilo.css`, bloco aditivo "FASE 3") — `.projeto-card` reaproveita a base visual de `.sobre-card`: fundo `--card-bg`, borda 1px `--card-border`, raio `12px`, `padding: 20px` (valor herdado, não ajustado à escala — FSD 12.3). Sem cor nova, sem sombra. Espaçamentos internos novos na escala (`6 · 8 · 16 · 24`). Cards ocupam a largura total da coluna (máx. `780px`) e são **empilhados** (`.projeto-card + .projeto-card { margin-top: 24px }`), nunca lado a lado.
- **Componente pílula de tecnologia** — `.tech-lista` (`<ul>` com `list-style: none`, `flex-wrap`, `gap: 8px`) + `.tech-pill` (`<li>`: `padding: 6px 12px`, raio `8px`, borda 1px `--card-border`, fundo `--card-bg`, peso 300). Hover: borda `--destaque`, `translateY(-2px)`. **Sem ícone, bolinha, barra, percentual ou nível** (RN16). Os itens **não são links**. Será reaproveitado na seção de tecnologias (Fase 7).
- **Card do HGS** — oito elementos na ordem fixa (B2): (1) nome `HGS — Hotel Governance System`; (2) uma linha do que é; (3) problema anterior; (4) o que faz; (5) o que mudou na rotina — **sem números**, redigido para aceitar um número depois sem reescrever o parágrafo (RN10); (6) onde está hoje (em produção; app em construção); (7) **placeholder textual** das imagens (Fase 5); (8) pílulas `PHP · MySQL · HTML · CSS · JavaScript · Git e GitHub · Figma` + nota "protótipo original em Java; app em construção com Flutter e Dart". Após o elemento 8, o botão **"Solicitar apresentação do sistema"** — `.btn-ghost`, `https://wa.me/5581995212456?text=…` com a mensagem específica do HGS, `target="_blank"` + `rel="noopener noreferrer"`. *[Atualizado em 2026-09-10: o texto do botão passou a "Pedir a gravação" e a mensagem passou a "...gostaria de receber a gravação do Hotel Governance System funcionando."; foi acrescentada a linha `.projeto-gravacao` acima do botão. Ver histórico.]*
- **Card do HFS** — os **mesmos oito elementos, na mesma ordem, com os mesmos rótulos** (B3): nome `HFS — Hotel Finance System`; uma linha do que é; problema; o que faz; o que mudou (sem números); onde está hoje (em uso, em fase de testes); placeholder de imagens; pílulas `PHP · MySQL · HTML · CSS · JavaScript · Git e GitHub`. **Sem nota e sem botão** (RN18 preservada — a nota e o botão não são elementos da estrutura de oito, são conteúdo extra previsto só para o HGS). *[Atualizado em 2026-09-10 (alteração consciente de escopo — FSD seção 1, item 4): o HFS passou a ter o botão **"Pedir a gravação"** e a linha `.projeto-gravacao`, iguais aos do HGS, com a mensagem que identifica o HFS. Continua sem a nota Java/Flutter (essa é só do HGS). RN18 segue preservada. Ver histórico.]*
- **Rótulo e título da seção** — `section-label` passou de "projeto principal" para "projetos"; `h2` passou de "Hotel Governance System" para **"Os sistemas"** (ver ajuste do autor abaixo). **Textos propostos** — a redação final dos oito elementos é do autor (PP03); ajustar direto na marcação.
- **CSS aditivo** — bloco "FASE 3" no fim de `css/estilo.css`; tudo consome variável de tema e funciona nos dois temas (RT02/RT04/RT05). Única remoção de CSS: o bloco `TIMELINE` (remoção autorizada — FSD 12.11). Nenhuma regra herdada foi reescrita. Chaves balanceadas (66/66).
- **`.btn-primary` continua aparecendo em um único ponto** (o "Ver projetos" do hero) — o botão do HGS é `.btn-ghost` (RN13). Na Fase 9 entra o segundo e último `.btn-primary` (WhatsApp do contato).

### Ajustes de conteúdo pedidos pelo autor (10/09/2026, após a Fase 3)

Nova regra registrada em `AGENTS.md` › **Regras de conteúdo**: *nenhuma frase, título ou informação aparece duas vezes com a mesma formulação em pontos diferentes da página.* Três repetições corrigidas:

- **Origem na recepção** — contava a mesma história no bloco de apresentação (abaixo do hero) e no card "quem sou" da seção Sobre. Mantido só no bloco de apresentação. O card "quem sou" será **reescrito na Fase 8** com outro recorte (trajetória, o que se estuda hoje, para onde se vai), sem recontar a origem na recepção — checklist adicionado ao `docs/PLANO.md` › Fase 8.
- **Título da seção Sobre** — era "Da recepção ao código", repetindo ao pé da letra a linha de posicionamento do hero. Trocado para **"Minha trajetória"** (provisório; a Fase 8 confirma).
- **"Dois sistemas em uso real"** — aparecia no selo do hero, no fim do parágrafo de apresentação e no título da seção Projetos. Ficou **exclusiva do selo do hero**: a última frase do parágrafo de apresentação ("Hoje mantenho dois sistemas que a equipe usa de verdade.") saiu, e o `h2` da seção Projetos passou a **"Os sistemas"**.
- **Procedimento de publicação registrado** em `AGENTS.md` › nova seção "Publicação (procedimento para a IA)": depois da confirmação explícita do autor ("conferi, pode publicar"), a IA executa sozinha a sequência `git push origin rascunho` → `git checkout main` → `git merge --no-ff rascunho` → `git push origin main` → `git checkout rascunho`, sem listar comandos para o autor. Sem essa confirmação, nada de `push`/`merge`/`checkout main`.

Arquivos tocados nesses ajustes: `index.html` (parágrafo de apresentação, `h2` de `#sobre`, `h2` de `#projetos`), `AGENTS.md` (novas seções "Regras de conteúdo" e "Publicação"), `docs/PLANO.md` (Fase 8).

### O que foi feito na Fase 2 (branch `rascunho`)

- **Foto real tratada** — `docs/foto_principal.jpeg` (1170×1438) recortada em quadrado enquadrando rosto e ombros, redimensionada para 320×320 e convertida para WebP (qualidade 90): `imagens/avatar.webp`, **12,6 KB** (teto por arquivo: 150 KB). Ferramenta local usada só para o tratamento: Pillow (Python) — **não é dependência do site**. `imagens/.gitkeep` removido (a pasta agora tem arquivo real).
- **Hero revisado** (`index.html`): `<img>` aponta para `imagens/avatar.webp` com `width`/`height` declarados (320×320) e **sem `loading="lazy"`** (o avatar não usa lazy — FSD 21.2); `alt="Retrato de João Adôrno"` (sem nome da pousada). Novo `h1` mantido ("João **Adôrno**"), nova linha de posicionamento em `.hero-sub` (**não menciona marketing** — A1), novo `.hero-selo` ("Dois sistemas em uso real numa pousada de 38 quartos em Maragogi/AL" — **não nomeia a pousada**, RN03). Botões: `.btn-primary` "Ver projetos" → `#projetos`; `.btn-ghost` "Falar comigo" → `#contato`. **Botão GitHub removido do hero** (vai para o contato na Fase 9). Hero **sem WhatsApp** (E2).
- **Apresentação curta** — novo bloco `.apresentacao` com um parágrafo de 3 linhas ("quem sou"), logo abaixo do hero (A3).
- **Estratégia de fontes** (FSD 24.3) — adicionado `preconnect` para `https://fonts.gstatic.com` (crossorigin); a URL do Google Fonts já tinha `display=swap` e só os pesos usados (Poppins 300/400/600/700, Space Mono 400/700); adicionada **pilha de fallback de sistema** nas famílias CSS (`'Poppins', -apple-system, …, sans-serif` e `'Space Mono', ui-monospace, …, monospace`) — inserção mecânica, sem reordenar nem remover regra.
- **CSS aditivo** — bloco "FASE 2" no fim de `css/estilo.css` com `.hero-selo` (pílula discreta, borda `--card-border`, fundo `--card-bg`, raio 8px, espaçamentos da escala) e `.apresentacao` / `.apresentacao p` (texto corrido, peso 300, `--texto-muted`). Tudo consome variável de tema e funciona nos dois temas (RT02/RT04/RT05). Nenhuma regra herdada foi reescrita.
- **Textos são propostos** — posicionamento, selo e apresentação seguem a direção do FSD, mas a redação final é do autor (PP03). Ajustar direto na marcação, se quiser.

### O que foi feito no trabalho de código da Fase 1 (branch `rascunho`)

- **Extração de CSS/JS (commit `9a9e6b1`)** — o conteúdo de `<style>` foi para `css/estilo.css` e o de `<script>` para `js/script.js`, por **cópia literal**: nenhuma regra renomeada, reordenada, reformatada ou "otimizada" (FSD 5.2). `index.html` referencia os dois por caminho relativo. Verificado: `index.html` + `css/estilo.css` + `js/script.js` reconstroem a base do commit `c311b26` **byte a byte**. Removidos os `.gitkeep` de `css/` e `js/` (agora com arquivos reais).
- **Navegação por âncora (commit `c0d082f`)** — menu com 4 itens (projetos, tecnologias, sobre, contato), centralizado, sem logo, cada link com `href="#id"` (FSD 5.3). Item "início" saiu do menu. IDs: `#inicio` no hero, `#stack` renomeado para `#tecnologias`, `<footer id="contato">` como alvo provisório até a seção de contato da Fase 9. Botões do hero viraram âncoras reais. **Todo `onclick` inline foi removido** — o botão de tema agora usa `addEventListener` em `js/script.js`. CSS aditivo isolado no fim de `estilo.css` (`scroll-behavior: smooth`, `scroll-margin-top`, âncora estilizada como botão), sem tocar nas regras herdadas (RT02). A navegação passou a **funcionar sem JavaScript** e a alternância de tema continua funcionando nos dois sentidos.
- **Inventário do código da base** registrado no `README.md` (FSD 25/Fase 1 item 3; PP04): seções, estrutura do HTML, organização do CSS, alternância de tema, componentes reaproveitáveis e o que sai. Conclusão: a base **suporta a troca de conteúdo sem retrabalho estrutural**.

### Pendência herdada da Fase 1

- [ ] **GitHub Pages + HTTPS** — configurar no painel do repositório `devdapraia/devdapraia.github.io` a publicação a partir do branch `main` (pasta raiz) e confirmar que o site responde em `https://devdapraia.github.io` sob HTTPS. Não é possível fazer isso por código/CLI neste ambiente. O código da Fase 1 já está no `main` (merge `5a0c80f`, enviado ao GitHub); falta só a configuração do Pages no painel.

### O que já havia sido feito antes desta etapa:

- Estrutura de pastas criada: `css/`, `js/`, `imagens/hgs/`, `imagens/hfs/`, `imagens/processo/`, `arquivos/`.
- `index.html` na raiz — cópia **literal e intacta** de `docs/index.html` (verificado byte a byte).
- `favicon.ico` gerado — quadrado, âmbar `#F29441`, tamanhos 16px e 32px.
- `README.md` e `.gitignore` criados.
- `AGENTS.md` criado na raiz.
- `docs/PLANO.md`, `docs/STATUS.md`, `docs/ERROS.md` criados.
- `arquivos/curriculo-joao-victor.pdf` — cópia de `docs/curriculo-joao-victor.pdf` (já conferido quanto a dados sensíveis; não requer tratamento).
- `docs/foto_principal.jpeg` **não** foi copiada: precisa de tratamento (recorte quadrado + WebP + redimensionamento) e será processada na Fase 2 (bloco de abertura).
- **`.gitignore` revisado** (rede de segurança para `.env`, chaves, certificados, dumps de banco, logs, lixo de SO/editor) e **`.gitattributes` criado** (fim de linha LF para texto/código; imagens e PDF como binário). Adaptados à stack estática — sem regras de PHP, Node, Python ou banco.
- **Repositório Git inicializado** e **primeiro commit** `c311b26` ("Estrutura inicial do projeto e configuração de controle de versão") com a base intacta — **ponto de retorno** do projeto (FSD 9.1).
- **Conferência anterior ao commit:** `git status` + busca por padrões de segredo (`senha`, `password`, `token`, `api_key`, chaves privadas) — nada encontrado. Nenhum `.env`, credencial, log, dump ou dado de terceiro entrou no repositório. `imagens/hgs/` e `imagens/hfs/` estão vazias (as capturas dos sistemas, ponto de risco real, ainda não existem).
- **Backup no GitHub:** remote `origin` → `https://github.com/devdapraia/devdapraia.github.io.git` (conexão HTTPS); `git push -u origin main` concluído. O repositório remoto foi criado **público** (condição do GitHub Pages gratuito) e **vazio**, sem conflito com os arquivos locais.

---

## Fases do PLANO e checklist por fase

Legenda: `[x]` concluído · `[~]` em andamento · `[ ]` não iniciado

### Fase 1 — Preservação da base, infraestrutura e preparação do terreno — `[~]` (código no `main`; só falta GitHub Pages + HTTPS, no painel do GitHub)

- [x] Estrutura de pastas (`css/`, `js/`, `imagens/hgs/`, `imagens/hfs/`, `imagens/processo/`, `arquivos/`)
- [x] `index.html` na raiz = cópia intacta de `docs/index.html`
- [x] `favicon.ico` gerado (âmbar `#F29441`, 16px + 32px)
- [x] `README.md` criado
- [x] `.gitignore` criado
- [x] `AGENTS.md` criado
- [x] `docs/PLANO.md`, `docs/STATUS.md`, `docs/ERROS.md` criados
- [x] `arquivos/curriculo-joao-victor.pdf` copiado de `docs/`
- [x] `.gitignore` revisado (rede de segurança para segredos) e `.gitattributes` criado (LF/binário), adaptados à stack estática
- [x] Repositório Git local inicializado + **primeiro commit com a base intacta** (`c311b26` — ponto de retorno)
- [x] Conferência de segredos antes do commit (`git status` + busca por padrões sensíveis) — nada versionado
- [x] Repositório remoto `devdapraia/devdapraia.github.io` criado no GitHub (nome exato, público, vazio) + `git push -u origin main`
- [x] Inventário do código da base registrado no `README.md` (PP04)
- [ ] **GitHub Pages configurado a partir de `main`, respondendo sob HTTPS** — *pendente, depende do autor (painel do GitHub); único item que falta na Fase 1*
- [x] Branch `rascunho` criado e ativo
- [x] CSS e JS extraídos para `css/estilo.css` e `js/script.js` (cópia literal, commit isolado `9a9e6b1`, reconstrução byte a byte verificada)
- [x] Navegação `onclick` substituída por âncoras reais (`href="#id"`, `id` nas seções, `scroll-behavior: smooth`, `scroll-margin-top`); menu com 4 itens (projetos, tecnologias, sobre, contato); todo `onclick` inline removido (commit `c0d082f`)

### Fase 2 — Bloco de abertura e apresentação curta — `[x]` (construída no `rascunho`; aguardando conferência visual do autor)

- [x] `docs/foto_principal.jpeg` tratada → `imagens/avatar.webp` (recorte quadrado, WebP 320×320, **12,6 KB** << 150 KB)
- [x] Novo `h1` mantido + nova linha de posicionamento (**sem mencionar marketing**); tag do hero preservada
- [x] Selo de credibilidade (`.hero-selo` — "dois sistemas em uso real numa pousada de 38 quartos em Maragogi/AL", **sem nomear a pousada**)
- [x] Botões do hero: `.btn-primary` "Ver projetos" → `#projetos`; `.btn-ghost` "Falar comigo" → `#contato`; **botão GitHub removido do hero**
- [x] Apresentação curta (`.apresentacao` — parágrafo de 3 linhas abaixo do hero)
- [x] Estratégia de fontes (`display=swap` já na URL + `preconnect` gstatic adicionado + pilha de fallback de sistema nas famílias CSS + só os pesos usados)
- [~] Hero visível "sem rolagem no celular" (critério 26.3): correto no desktop; o empilhamento em telas estreitas é entregue na **Fase 10 (Responsividade)** — a base não tem media queries e o PLANO sequencia isso para a Fase 10.

### Fase 3 — Cards de projeto (HGS e HFS): estrutura e texto — `[x]` (construída no `rascunho`; aguardando conferência visual do autor)

- [x] Remover `.timeline` e `.timeline-item` (marcação + CSS), após verificar que nada mais os usa (o observer de `js/script.js` fica para a Fase 10)
- [x] Componente de card de projeto (base visual de `.sobre-card`, sem cor nova, sem sombra, empilhado)
- [x] Componente pílula de tecnologia (elemento 8 do card) — sem ícone/barra/nível, não é link
- [x] Card do HGS — oito elementos na ordem fixa + nota Java/Flutter+Dart + botão de contato (`.btn-ghost`, WhatsApp com mensagem do HGS) + linha `.projeto-gravacao` (ajuste 2026-09-10: texto do botão passou a "Pedir a gravação")
- [x] Card do HFS — os mesmos oito elementos, mesma ordem, mesmos rótulos. ~~sem botão~~ **Ajuste fora de fase 2026-09-10 (alteração consciente de escopo — FSD seção 1, item 4):** recebeu o botão **"Pedir a gravação"** (`.btn-ghost`, WhatsApp com mensagem do HFS) + a linha `.projeto-gravacao`, igual ao HGS. RN18 preservada (conteúdo extra, fora dos 8 elementos).
- [x] "O que mudou" sem números não apurados, redigido para aceitar número depois (RN10)
- [~] Elemento 7 (imagens) com **placeholder textual** — as quatro capturas conferidas entram na **Fase 5**; o card só vai ao ar pleno depois disso (FSD 12.3)

### Fase 4 — Limpeza da autoavaliação de nível — `[x]` (construída no `rascunho`; aguardando conferência visual do autor)

- [x] Remover `.stack-item` e `.tech-dot` (marcação + CSS), após verificar que nada mais os usa
- [x] Confirmar que a pílula de tecnologia cobre o uso restante
- [x] Nenhuma barra/percentual/estrela/bolinha/ícone graduado remanescente

### Fase 5 — Imagens de tela e ampliação ao toque — `[x]` (construída no `rascunho`; aguardando conferência visual do autor)

- [x] Conferência imagem por imagem, em tamanho real, contra dado de terceiro (antes do commit) — HGS: só número de quarto/status/alerta; HFS: valores fictícios (confirmado pelo autor). Sem nome de hóspede, documento, telefone, nome de pousada ou credencial. 2ª conferência = teste em celular do autor.
- [x] Conversão WebP, largura máx. 1200px, teto 150 KB/arquivo — 4 arquivos entre 55,8 e 86,7 KB, 1170×2150 (recorte da barra de status e da barra do navegador)
- [x] HGS: 2 capturas (dados fictícios) em `imagens/hgs/` — `grade-de-quartos.webp`, `central-de-alertas.webp`
- [x] HFS: 2 capturas em `imagens/hfs/` — `contas-vencidas.webp`, `lancamentos.webp`. Valores **fictícios**, publicados sem tarja (decisão consciente do autor — ver topo do arquivo)
- [x] Marcação: `width`, `height`, `loading="lazy"`, `alt` sem dado de hóspede/pousada, borda 1px `--card-border` e raio `12px`
- [~] Disposição responsiva (2 colunas → empilhado) — 2 colunas entregues; o empilhamento em telas estreitas é da **Fase 10** (a base não tem media queries)
- [x] Ampliação ao toque: `<a>` para o arquivo (funciona sem JS) + overlay em `js/script.js` (fecha por clique no fundo, botão `×` e `Esc`; trava a rolagem do corpo com `body.overlay-aberto`)

### Fase 6 — Bloco de processo de design (Figma) — `[x]` (construída no `rascunho`; aguardando conferência visual do autor)

- [x] Seção autocontida `id="processo"` após os cards (rótulo "processo de design" + `h2` "Protótipo e design system do app" + `.processo-intro` + 3 recortes)
- [x] Imagens do Figma em `imagens/processo/` com as regras técnicas da Fase 5 (`width`/`height` `1170`×`2382`, `loading="lazy"`, `alt` sem dado de hóspede/pousada, ampliação ao toque via `.projeto-imagem`; WebP q82, 75,6–79,2 KB). Conferência de dado de terceiro feita — mockups de protótipo, nomes genéricos de teste; confirmação final do autor na conferência visual
- [x] Marcação e estilo autocontidos — bloco `FASE 6` delimitado por comentários em `index.html` (inclui o `<hr>` que o precede) e bloco `FASE 6` em `css/estilo.css` (com media queries próprias); remover volta ao estado da Fase 5 sem efeito colateral

### Fase 7 — Seção de tecnologias e linha de formação — `[ ]`

- [ ] Seção `#tecnologias`: rótulo + `h2` + dois grupos com os itens exatos (C2)
- [ ] Itens como pílulas, sem indicação de nível; títulos de grupo em Space Mono / `--acento`
- [ ] Linha de formação: ADS, conclusão prevista para junho de 2028
- [ ] Verificação de lastro (RN09): toda tecnologia do 1º grupo aparece nos cards

### Fase 8 — Seção "Sobre mim" — `[ ]`

- [ ] Seção `#sobre`: rótulo + `h2` + três parágrafos em texto corrido (sem card, sem imagem)
- [ ] Sequência recepção → problema observado → decisão de estudar ADS e construir
- [ ] Marketing em no máximo uma frase
- [ ] Frase do ERBASE 2026 no 3º parágrafo (sem afirmar aceitação, sem desculpas)
- [ ] Sem menção a Recife nem Porto Digital

### Fase 9 — Seção de contato e conversão — `[ ]`

- [ ] Seção `#contato` em tela cheia com pergunta convidativa
- [ ] WhatsApp em `.btn-primary` — único botão em destaque da seção
- [ ] E-mail, LinkedIn, GitHub e download do currículo (`download`, sem formulário) subordinados
- [ ] Links externos com `target="_blank"` + `rel="noopener noreferrer"`
- [ ] Conferir que `.btn-primary` só aparece em dois pontos da página
- [ ] Testar todos os links, um a um

### Fase 10 — Responsividade e revelação por rolagem — `[ ]`

- [ ] Media queries de `768px` e `480px` (tabela por componente da seção 12.8)
- [ ] Conferência a partir de 360px: sem rolagem horizontal em nenhuma faixa
- [ ] Revelação por rolagem reapontada, com `opacity: 0` aplicado só por JS (ou removida)

### Fase 11 — Desempenho, segurança, qualidade e entrega — `[ ]`

- [ ] Tetos de peso (≤ 150 KB/imagem; ≤ 1,5 MB por página); fontes; sem animação pesada
- [ ] Remover `console.log` e código morto
- [ ] Revisão de segurança na ordem da seção 24.4
- [ ] `rel="noopener noreferrer"` em todos os links de nova aba; sem script de terceiro/cookie/analytics
- [ ] Revisão de qualidade — seção 26, item a item
- [ ] Conferência final em celular real
- [ ] Entrega: `rascunho` → `main`, conferir endereço publicado, atualizar link no LinkedIn e no GitHub

---

## Histórico de atualizações

- **2026-09-10** — Preparação do terreno. Criados `docs/PLANO.md`, `AGENTS.md`, `docs/STATUS.md`, `docs/ERROS.md`, `README.md`, `.gitignore`, `favicon.ico` e a estrutura de pastas. `index.html` copiado intacto para a raiz; currículo copiado para `arquivos/`. Fase 1 iniciada e parcialmente concluída (infraestrutura de arquivos pronta; Git, GitHub Pages, extração de CSS/JS e navegação por âncora ainda pendentes).
- **2026-09-10** — Trabalho de código da Fase 1 (branch `rascunho`). (1) Extração de CSS/JS embutidos para `css/estilo.css` e `js/script.js` por cópia literal, em commit isolado `9a9e6b1`; reconstrução da base do commit `c311b26` verificada byte a byte. (2) Navegação por âncora real no lugar do `onclick` inline, commit `c0d082f`: menu de 4 itens com `href="#id"`, `id` nas seções (`#inicio`, `#tecnologias` no lugar de `#stack`, `#contato` provisório no `<footer>`), botões do hero como âncoras, botão de tema via `addEventListener`, CSS aditivo isolado (`scroll-behavior`, `scroll-margin-top`, âncora-botão) sem tocar em regra herdada. Navegação passou a funcionar sem JavaScript; alternância de tema mantida. (3) Inventário do código da base no `README.md` (PP04) — base suporta a troca de conteúdo sem retrabalho estrutural. **Pendência da Fase 1:** configurar GitHub Pages a partir de `main` e confirmar HTTPS (depende do autor). Nada foi integrado ao `main` nem enviado ao GitHub ainda.
- **2026-09-10** — Controle de versão e backup. `.gitignore` revisado (rede de segurança para `.env`, chaves, certificados, dumps, logs, lixo de SO/editor) e `.gitattributes` criado (LF para texto/código, binário para imagens e PDF), ambos adaptados à stack estática. `git init` + primeiro commit `c311b26` com a base intacta (ponto de retorno). Conferência de segredos antes do commit: nada sensível versionado. Remote `origin` (HTTPS) apontado para `github.com/devdapraia/devdapraia.github.io` e `git push -u origin main` concluído — backup no GitHub feito. Pendências da Fase 1: GitHub Pages + HTTPS, inventário do código no `README.md`, branch `rascunho`, extração de CSS/JS e navegação por âncora.
- **2026-09-10** — Fase 1 integrada ao `main` (merge `5a0c80f`) e enviada ao GitHub (`origin/main` e `origin/rascunho` sincronizados). Pendência restante da Fase 1: apenas GitHub Pages + HTTPS no painel do GitHub.
- **2026-09-10** — **Título de `#projetos` + procedimento de publicação** (branch `rascunho`). `h2` da seção Projetos "Dois sistemas em uso real" → **"Os sistemas"** (a frase "dois sistemas em uso real" ficou exclusiva do selo do hero). Registrado em `AGENTS.md` › "Publicação (procedimento para a IA)": após o "conferi, pode publicar" do autor, a IA executa sozinha `git push origin rascunho` / `git checkout main` / `git merge --no-ff rascunho` / `git push origin main` / `git checkout rascunho`, sem listar comandos; o portão continua sendo do autor. Arquivos: `index.html`, `AGENTS.md`, `docs/STATUS.md`. Sem CSS/JS. Smoke test local: `/`, `css/estilo.css`, `js/script.js`, `imagens/avatar.webp`, `favicon.ico` → **200**; sem `style=`/`onclick=`/caminho absoluto; `.btn-primary` 1×, `.btn-ghost` 2×.
- **2026-09-10** — **Ajustes de conteúdo do autor sobre a Fase 3** (branch `rascunho`). Nova regra em `AGENTS.md` › "Regras de conteúdo": nenhuma frase/título/informação se repete com a mesma formulação em pontos diferentes da página. (1) Origem na recepção mantida só no bloco de apresentação; card "quem sou" da seção Sobre a reescrever na Fase 8 com outro recorte — checklist adicionado ao `docs/PLANO.md` › Fase 8. (2) `h2` de `#sobre` "Da recepção ao código" → "Minha trajetória" (provisório; repetia a linha do hero). (3) "Dois sistemas em uso real" retirado do parágrafo de apresentação (frase final removida); mantido no selo do hero e no título de `#projetos`. Arquivos: `index.html`, `AGENTS.md`, `docs/PLANO.md`, `docs/STATUS.md`. Sem alteração de CSS/JS. Testes locais repetidos: `/`, `css/estilo.css`, `js/script.js`, `imagens/avatar.webp`, `favicon.ico`, currículo em PDF → **200**; sem `style=`/`onclick=`/caminho absoluto; `.btn-primary` 1×, `.btn-ghost` 2×.
- **2026-09-10** — **Fase 3 — Cards de projeto (HGS e HFS): estrutura e texto** (branch `rascunho`, sobre `ad1ad13`). Linha do tempo removida: `.timeline`/`.timeline-item` saíram da marcação de `#projetos` (`index.html`) e do CSS (bloco `TIMELINE` de `css/estilo.css`) — `grep` confirmou que nada mais os usa em arquivo vivo; o `IntersectionObserver` de `js/script.js` foi mantido intacto (decisão de reapontar/remover é da Fase 10). Criado o componente `.projeto-card` (base visual de `.sobre-card`: `--card-bg`, borda 1px `--card-border`, raio 12px, padding 20px herdado; sem cor nova, sem sombra; espaçamentos novos na escala; cards empilhados, nunca lado a lado) e o componente pílula de tecnologia `.tech-lista`/`.tech-pill` (`<ul>`/`<li>` com `list-style: none`, `flex-wrap`, peso 300, hover borda `--destaque` + `-2px`; sem ícone/bolinha/barra/percentual/nível — RN16; não são links). Card do HGS: oito elementos na ordem fixa (B2) + nota "protótipo original em Java; app em construção com Flutter e Dart" abaixo das pílulas + botão "Solicitar apresentação do sistema" (`.btn-ghost`, `wa.me` com a mensagem do HGS, `target="_blank"` + `rel="noopener noreferrer"`). Card do HFS: os mesmos oito elementos, mesma ordem, mesmos rótulos, sem nota e sem botão (B3, RN18). "O que mudou" nos dois cards descrito por mudança de rotina, sem números, redigido para aceitar um número depois (RN10). Elemento 7 (imagens) com placeholder textual temporário — as capturas entram na Fase 5. `section-label` "projeto principal" → "projetos"; `h2` "Hotel Governance System" → "Dois sistemas em uso real". CSS aditivo no bloco "FASE 3" do fim de `css/estilo.css`, consumindo variáveis de tema; chaves balanceadas (66/66); nenhuma regra herdada reescrita. Testes locais (`python -m http.server`): `/`, `css/estilo.css`, `js/script.js`, `imagens/avatar.webp`, `favicon.ico`, currículo em PDF respondem **200**; sem `style=`/`onclick=`/caminho absoluto na marcação; `.btn-primary` aparece **1×** (só o hero), `.btn-ghost` **2×** (hero + botão do HGS); peso total dos arquivos servidos ≈ 45 KB (teto 1,5 MB). Sem erros registrados. **Ressalva:** cards com placeholder de imagem — publicação plena depende da Fase 5. Aguardando conferência visual do autor no Live Server / celular; depois: push do `rascunho`, merge em `main`, push do `main`, retorno ao `rascunho`. **Próxima fase: Fase 4 — Limpeza da autoavaliação de nível.**
- **2026-09-10** — **Fase 4 — Limpeza da autoavaliação de nível** (branch `rascunho`, sobre `2c8f264`). Removida a autoavaliação de nível de habilidade da base: a marcação `.stack-grid` / `.stack-item` / `.tech-dot` (com `tech-name` e `tech-level` "avançado", "intermediário", "básico", "iniciando", "em breve") saiu da seção `#tecnologias` de `index.html`; o bloco CSS `STACK` inteiro (`.stack-grid`, `.stack-item`, `.stack-item:hover`, `.stack-item .tech-name`, `.stack-item .tech-level`, `.tech-dot`, `.dot-on`, `.dot-off`) saiu de `css/estilo.css`. `grep` em `index.html` + `css/estilo.css` + `js/script.js` confirmou que nada mais usa essas classes (ocorrências restantes só em comentários de documentação). A seção `#tecnologias` ficou publicável e íntegra com rótulo + `h2` "Stack atual" (provisório) + `p.secao-preparo` "Conteúdo em preparação." — os dois grupos por contexto de uso e a linha de formação entram na Fase 7, reaproveitando a pílula de tecnologia da Fase 3. CSS aditivo "FASE 4" no fim de `css/estilo.css` só com `.secao-preparo` (peso 300, `--texto-muted`, consome variável de tema). Única remoção de CSS: o bloco `STACK` (autorizada — FSD 12.11); nenhuma regra herdada reescrita; chaves balanceadas (59/59). `js/script.js` não foi tocado. `README.md` atualizado (inventário do código da base). Testes locais (`python -m http.server`): `/`, `css/estilo.css`, `js/script.js`, `imagens/avatar.webp`, `favicon.ico`, currículo em PDF → **200**; sem `style=`/`onclick=`/caminho absoluto na marcação; `.btn-primary` **1×** (hero), `.btn-ghost` **2×** (hero + botão do HGS); peso dos arquivos servidos ≈ 56 KB (teto 1,5 MB). Sem erros registrados. Aguardando conferência visual do autor no Live Server / celular; depois: push do `rascunho`, merge em `main`, push do `main`, retorno ao `rascunho`. **Próxima fase: Fase 5 — Imagens de tela e ampliação ao toque.**
- **2026-09-10** — **Ordem das seções + card "objetivo"** (branch `rascunho`, ajuste fora de fase a pedido do autor). (1) **Menu × ordem da página:** verificado que o FSD registra a ordem de leitura em quatro pontos concordantes (seção 1 "Resumo do funcionamento"; 5.3 tabela de IDs; 13/Fluxo 1; 25/Fase 2 itens 19–21) — **`#projetos` antes de `#tecnologias` antes de `#sobre`** — e que o menu (`projetos · tecnologias · sobre · contato`) já estava nessa ordem. O que divergia era a **página**: `#sobre` vinha da base **antes** de `#projetos`. Decisão do autor: mover `#sobre` para **depois de `#tecnologias`**, alinhando o código ao FSD; nenhum documento mudou de ordem. Em `index.html`, o bloco `<section id="sobre">` foi realocado para logo antes do `<footer id="contato">`, com um `<hr class="divider">` entre `#tecnologias` e `#sobre`; contagem de dividers inalterada (2). O menu não mudou. (2) **Card "objetivo":** texto herdado da base `Estágio no Porto Digital · Recife · Jan 2027` → `Primeira oportunidade na área de desenvolvimento de software` — indica a direção sem fixar destino nem data (o site é lido por empresas diferentes; um destino datado sinaliza intenção de sair). A formulação antiga **não estava registrada no FSD**; o FSD 7.3 e a linha 357 já mandavam não mencionar Recife nem Porto Digital, então a troca antecipa o que a Fase 8 faria. Registrado no checklist da Fase 8 do `docs/PLANO.md` (dois itens `[x]`, feitos antecipadamente) e no inventário do `README.md`. Arquivos: `index.html`, `docs/PLANO.md`, `docs/STATUS.md`, `README.md`. Sem alteração de CSS/JS. Testes locais (`python -m http.server`): `/`, `css/estilo.css`, `js/script.js`, `imagens/avatar.webp`, `favicon.ico`, currículo em PDF → **200**; ordem no HTML = hero → apresentação → `#projetos` → `#tecnologias` → `#sobre` → `#contato`; âncoras do menu (`#projetos`, `#tecnologias`, `#sobre`, `#contato`) todas com alvo existente; sem `style=`/`onclick=`/caminho absoluto; `.btn-primary` 1×, `.btn-ghost` 2×; sem "Porto Digital"/"Recife"/"Jan 2027" na marcação. Sem erros registrados.
- **2026-09-10** — **Ordem final das seções/menu + posicionamento marketing/desenvolvimento** (branch `rascunho`, alteração consciente de escopo a pedido do autor). **PARTE 1 — ordem.** Revertido o passo anterior: `#sobre` volta para **antes de `#projetos`**. Ordem final da página: hero → apresentação → `#sobre` → `#projetos` → `#tecnologias` → `#contato`. O `<nav>` foi reordenado para **`sobre · projetos · tecnologias · contato`** (o CSS herdado sobe para maiúsculas). Em `index.html`: bloco `<section id="sobre">` movido para logo após `.apresentacao`, com `<hr class="divider">` entre `#sobre` e `#projetos`; o divider que estava entre `#tecnologias` e `#sobre` saiu; contagem de dividers inalterada (2). Isso **contraria o FSD** nas seções 1, 5.3, 12.1, 13 (Fluxo 1) e 25 — tratado como **alteração consciente de escopo** e registrado nos cinco pontos + em `AGENTS.md` › Regras de conteúdo. *Motivo:* o site é lido também por quem chega sem contexto prévio; a apresentação antes dos projetos dá esse contexto. Critério novo na Fase 8: como "Sobre" abre a página, precisa ser **curto**. **PARTE 2 — posicionamento marketing + desenvolvimento.** A oportunidade pode ser em desenvolvimento **ou** em marketing (o público principal são gestores de agência de comunicação). Card "formação" da seção Sobre: `ADS · 2º período · Anhanguera · Maragogi` → `Análise e Desenvolvimento de Sistemas — conclusão prevista para junho de 2028` **+** `Bacharelado em Marketing — UNIBRA, 2020` (Marketing ao lado de ADS, como parte do perfil). Card "objetivo": `Primeira oportunidade na área de desenvolvimento de software` → `Uma posição que una repertório de negócio e capacidade de construir a solução, em desenvolvimento ou em marketing` (serve às duas frentes). Card "quem sou": recorte provisório reescrito para abrir pela origem em marketing/atendimento/coordenação. O `h1`/linha do hero **não** mudaram (servem às duas leituras); a avaliação do hero fica como item da Fase 8. Isso **contraria o FSD** em 6/A1, 6/D1, 10 e 26.5 — registrado nesses pontos + em `AGENTS.md`. Nenhum PRD existe no repositório (só citado em `DESIGN.md`); o FSD é a autoridade. Arquivos: `index.html`, `docs/FSD.md`, `docs/PLANO.md`, `AGENTS.md`, `README.md`, `docs/STATUS.md`. Sem alteração de CSS/JS. Testes locais (`python -m http.server`): `/`, `css/estilo.css`, `js/script.js`, `imagens/avatar.webp`, `favicon.ico`, currículo em PDF → **200**; ordem no HTML = hero → apresentação → `#sobre` → `#projetos` → `#tecnologias` → `#contato`; ordem do `<nav>` = sobre, projetos, tecnologias, contato; âncoras do menu com alvo existente; sem `style=`/`onclick=`/caminho absoluto; `.btn-primary` 1×, `.btn-ghost` 2×; sem "Porto Digital"/"Recife"/"Jan 2027" na marcação. Sem erros registrados.
- **2026-09-10** — **Bloco `.apresentacao` removido e unificado no card "quem sou"** (branch `rascunho`, alteração consciente de escopo a pedido do autor). O bloco `.apresentacao` abaixo do hero (criado na Fase 2, FSD 6/A3) **saiu de `index.html`** e o CSS `.apresentacao` / `.apresentacao p` **saiu de `css/estilo.css`** (`grep` em `index.html` + `css/estilo.css` + `js/script.js` confirmou que nada mais usa — só sobra a palavra em nome de arquivo/`docs`). O texto de "quem sou" foi **unificado no card "quem sou"** da seção Sobre, com a redação passada pelo autor: *"Vim do marketing, do atendimento e da coordenação de equipe. Hoje trabalho na recepção de uma pousada, estudo Análise e Desenvolvimento de Sistemas e construo software a partir dos problemas que vejo travarem a operação todo dia."* Ordem da página agora: **hero → `#sobre` → `#projetos` → `#tecnologias` → `#contato`** (sem bloco entre o hero e o `#sobre`). *Motivo:* com "Sobre mim" abrindo a página logo após o hero, um bloco de apresentação separado imediatamente antes seria redundância na mesma dobra. **Contraria o FSD** em 6/A3 (módulo descontinuado), 1 (item 3 da alteração consciente), 5.3, 12.7, 25 (item 10) e 26.5 — todos atualizados; `AGENTS.md` › Regras de conteúdo e `README.md` também. Arquivos: `index.html`, `css/estilo.css`, `docs/FSD.md`, `docs/PLANO.md`, `AGENTS.md`, `README.md`, `docs/STATUS.md`. CSS: única remoção foi `.apresentacao` (bloco "FASE 2" mantém `.hero-selo`); nenhuma regra herdada reescrita; chaves balanceadas. Testes locais (`python -m http.server`): `/`, `css/estilo.css`, `js/script.js`, `imagens/avatar.webp`, `favicon.ico`, currículo em PDF → **200**; ordem no HTML = hero → `#sobre` → `#projetos` → `#tecnologias` → `#contato`; sem `class="apresentacao"` na marcação nem `.apresentacao` no CSS; âncoras do menu com alvo existente; sem `style=`/`onclick=`/caminho absoluto; `.btn-primary` 1×, `.btn-ghost` 2×; dividers 2. Sem erros registrados.
- **2026-09-10** — **Fase 2 — Bloco de abertura e apresentação curta** (branch `rascunho`, sobre `5a0c80f`). Foto real tratada com Pillow → `imagens/avatar.webp` (recorte quadrado 320×320, WebP q90, 12,6 KB); `imagens/.gitkeep` removido. Hero revisado em `index.html`: `<img>` para `avatar.webp` com `width`/`height` e sem `lazy`, `alt` sem nome da pousada; nova linha de posicionamento (sem marketing); novo `.hero-selo` (uso real, sem nomear a pousada); botões "Ver projetos" (`.btn-primary` → `#projetos`) e "Falar comigo" (`.btn-ghost` → `#contato`); botão GitHub removido do hero; hero sem WhatsApp. Novo bloco `.apresentacao` (3 linhas) abaixo do hero. Estratégia de fontes: `preconnect` gstatic adicionado, pilha de fallback de sistema nas famílias CSS, `display=swap` e pesos já corretos na URL. CSS aditivo "FASE 2" no fim de `css/estilo.css` (`.hero-selo`, `.apresentacao`) — consome variáveis de tema, funciona nos dois temas, sem reescrever regra herdada. Testes locais: `py -m http.server` — `/`, `css/estilo.css`, `js/script.js`, `imagens/avatar.webp`, `favicon.ico`, currículo em PDF respondem 200; peso da página 38,5 KB (teto 1,5 MB); avatar 12,6 KB (teto 150 KB); sem `style=`/`onclick=`/caminho absoluto na marcação. Sem erros registrados. **Ressalva:** empilhamento do hero no celular fica para a Fase 10 (a base não tem media queries). Aguardando conferência visual do autor no Live Server / celular; depois: push do `rascunho`, merge em `main`, push do `main`, retorno ao `rascunho`.
- **2026-09-10** — **Botão "Pedir a gravação" nos dois cards** (branch `rascunho`, ajuste fora de fase a pedido do autor, alteração consciente de escopo). **Contexto:** existem gravações de tela do HGS e do HFS funcionando (`apresentacao-hgs.mp4` ≈ 37 MB, `apresentacao-hfs.mp4` ≈ 57 MB, mais `figma-1/2/3.png`), todas soltas na raiz. Decisão (caminho B): **os vídeos não entram no site** — teto de peso total 1,5 MB (FSD 21.2) e vídeo é fora de escopo (FSD 7.1) / sem incorporação (FSD 24.2). `.gitignore` recebeu `*.mp4`, `*.mov`, `*.webm`, `/figma-*.png`, `/apresentacao-*.png` — `git check-ignore` confirma que os 5 arquivos ficam fora do versionamento. **O que mudou na página** (`index.html`): (1) no card do HGS, o botão passou de "Solicitar apresentação do sistema" para **"Pedir a gravação"** e a mensagem de WhatsApp passou a `...gostaria de receber a gravação do Hotel Governance System funcionando.`; (2) o card do HFS **ganhou** o mesmo botão `.btn-ghost` "Pedir a gravação", com a mensagem `...do Hotel Finance System funcionando.`; (3) os dois cards ganharam, acima do botão, a linha `<p class="projeto-gravacao">` ("Gravei o [sistema] funcionando no meu celular. Toque no botão e eu te envio a gravação pelo WhatsApp — é um envio pessoal, feito por mim."). **CSS** (`css/estilo.css`): bloco aditivo com `.projeto-gravacao` (mesmo tratamento de `.tech-nota`: 0.8em, peso 300, `--texto-muted`) e `.projeto-gravacao + .btn-ghost { margin-top: 8px }`. Nenhuma regra herdada reescrita; chaves 59/59. **`js/script.js` não tocado.** **Conflito com o FSD (registrado como alteração consciente de escopo — FSD seção 1, item 4):** 6/B3, 6/C1, 12.3, 13/Fluxo 3, 21.1, 7.1, 24.4, 26.4, 26.6, RN06, RN07, RN13 e persona técnica (seção 8) — todos anotados. **RN18 preservada** (botão e linha são conteúdo extra, não elementos da estrutura de oito). **RN13 preservada** (`.btn-primary` continua só em "Ver projetos" e no WhatsApp do contato). Docs atualizados: `docs/FSD.md`, `docs/INSUMOS.md` (§2 — mensagens), `docs/PLANO.md` (Fase 3), `AGENTS.md` (Regras de conteúdo), `.gitignore`, `docs/STATUS.md`. **Testes locais** (`python -m http.server`): `/`, `css/estilo.css`, `js/script.js`, `imagens/avatar.webp`, `favicon.ico`, currículo em PDF → **200**; `.btn-primary` **1×**, `.btn-ghost` **3×** (hero + 2 cards); "Pedir a gravação" **2×**; sem `style=`/`onclick=`/caminho absoluto; chaves CSS 59/59; os 5 arquivos de mídia **ignorados** pelo Git. Sem erros registrados. Este ajuste **não abre nem fecha fase** — a Fase 4 segue aguardando conferência do autor e a Fase 5 segue travada nas 4 capturas de tela.
- **2026-09-10** — **Fase 5 — Imagens de tela e ampliação ao toque** (branch `rascunho`, sobre `5c5a5df`). As 4 capturas brutas (`hgs-1/2.png`, `hfs-1/2.png`, 1170×2532, soltas na raiz) foram conferidas em tamanho real contra dado de terceiro (FSD 21.4): HGS só com número de quarto/status/alerta, HFS com valores **fictícios / de teste** (confirmado pelo autor no chat — decisão consciente registrada no topo do `STATUS.md`: publicadas sem tarja, atende 26.4, leitura mais frouxa de "mascarados" em 21.1/RN02). Sem nome de hóspede, documento, telefone, nome de pousada, funcionário ou credencial. Recorte de 150px no topo (barra de status iOS) e 232px na base (barra do Safari + home indicator) — a URL `hfs.web.br.com` que aparecia saiu no recorte. Conversão para WebP q82 com Pillow (não é dependência do site): `imagens/hgs/grade-de-quartos.webp` 57,2 KB, `imagens/hgs/central-de-alertas.webp` 86,7 KB, `imagens/hfs/contas-vencidas.webp` 55,8 KB, `imagens/hfs/lancamentos.webp` 68,6 KB (todas 1170×2150, << 150 KB). `.gitignore`: `/hgs-*.png` e `/hfs-*.png` adicionados (capturas brutas ficam fora do histórico); `.gitkeep` removido de `imagens/hgs/` e `imagens/hfs/`. `index.html`: o placeholder textual do elemento 7 dos dois cards deu lugar a dois `<a class="projeto-imagem" ... target="_blank" rel="noopener">` (FSD 12.4) com `<img width="1170" height="2150" loading="lazy" alt="…">`. CSS aditivo "FASE 5" no fim de `css/estilo.css`: grade de 2 colunas (`gap: 16px`), borda/raio das imagens, sobreposição `.img-overlay` (fundo `rgba(10,7,18,0.92)` escuro nos dois temas de propósito), `.img-overlay-fechar` (botão redondo 44px, padrão do `.theme-toggle`), `body.overlay-aberto { overflow: hidden }`. Única remoção de CSS: `.projeto-imagens-placeholder` (andaime temporário da própria Fase 3 — critério 26.2); nenhuma regra herdada reescrita; chaves 68/68. `js/script.js`: bloco "FASE 5" em IIFE — intercepta o clique, abre a sobreposição via `document.createElement` (sem `innerHTML`), fecha por clique no fundo / botão `×` / `Esc`, trava a rolagem do corpo; sem JavaScript, o `<a>` ainda abre a imagem em nova aba. `IntersectionObserver` herdado não foi tocado (Fase 10). **Testes locais** (`python -m http.server`): `/`, `css/estilo.css`, `js/script.js`, `favicon.ico`, os 4 `.webp`, `imagens/avatar.webp`, currículo em PDF → **200**; `node --check js/script.js` OK; sem `style=`/`onclick=`/caminho absoluto; `.btn-primary` **1×**, `.btn-ghost` **3×**; peso total dos assets servidos **317,3 KB** (teto 1,5 MB); maior imagem 86,7 KB (teto 150 KB); `git check-ignore` confirma as 4 capturas brutas fora do versionamento. Sem erros registrados. **Ressalva:** empilhamento das imagens em telas estreitas fica para a Fase 10. Aguardando conferência visual do autor no Live Server / celular; depois: push do `rascunho`, merge em `main`, push do `main`, retorno ao `rascunho`. **Próxima fase: Fase 6 — Bloco de processo de design (Figma).**
- **2026-09-10** — **Ajuste fora de fase — responsividade do mobile** (branch `rascunho`, sobre `40414f7`, a pedido do autor com prints do celular). Entrou o **primeiro CSS responsivo do projeto**: bloco "AJUSTE DE RESPONSIVIDADE" no fim de `css/estilo.css`, só com `@media (max-width: 768px)` e `@media (max-width: 480px)` — 100% aditivo, nenhuma regra herdada reescrita; desktop (> 768px) byte a byte igual. **768px:** `nav` com `gap: 16px`, `padding: 16px 24px`, `flex-wrap: wrap` e `nav a` a `0.8em` (o menu deixou de encostar/cortar nas bordas); `.hero-content` vira coluna (`flex-direction: column`, `gap: 24px`, `text-align: center`) — a foto fica **acima** do `h1`/descrição, tudo centralizado e com respiro; `.hero-avatar` `130px`; `.hero h1` `2.2em`; `.hero-btns` centralizado; `.section`/`.divider`/`footer` com recuo lateral `24px` (mesma margem do hero); `.sobre-grid` e `.projeto-imagens` passam a **uma coluna** (fim do desalinhamento entre os cards "quem sou" x "formação"/"objetivo" e das imagens espremidas). **480px:** `nav` `gap: 12px`/`padding: 14px 16px`; hero `padding` menor; `.hero-avatar` `110px`; `.hero h1` `1.9em`; `.hero-selo` `0.78em`; recuos laterais a `16px`; botões do hero ocupam a largura toda quando quebram. **Testes:** `python -m http.server` → `/` e `css/estilo.css` **200**; chaves CSS **91/91**; `@media` **2**. **Não fecha a Fase 10** — a varredura a partir de 360px, o resto da tabela do FSD 12.8, o `scroll-margin-top` do menu ao quebrar em duas linhas e a revelação por rolagem seguem para a Fase 10. **Próxima fase planejada continua sendo a Fase 6.** Ainda no `rascunho`, sem push (aguardando "conferi, pode publicar").
- **2026-09-10** — **Publicação: `rascunho` → `main`** (autor: "conferi, pode publicar"). `git push origin rascunho` (backup dos commits `40414f7` e `339c22d`), depois `git checkout main` → `git merge --no-ff rascunho` (merge `d19b106`, mensagem "Publica Fase 5 (imagens de tela + ampliacao) e ajuste de responsividade do mobile") → `git push origin main` (`3fe40ae..d19b106`) → `git checkout rascunho`. Com isso as **Fases 2, 3, 4 e 5 + o ajuste de responsividade do mobile** ficaram no `main`; `origin/main` e `origin/rascunho` sincronizados. Conferência prévia: `git status` limpo, nenhum arquivo bruto/sensível no merge (`*.mp4`, `hgs-*.png`, `hfs-*.png`, `figma-*.png` ignorados; confirmado). **Pendência que continua:** o GitHub Pages a partir de `main` + HTTPS ainda não foi ligado no painel do GitHub (item herdado da Fase 1) — até isso, o `main` está atualizado no repositório mas o site não responde na URL pública.
- **2026-09-10** — **Fase 6 — Bloco de processo de design (Figma)** (branch `rascunho`, sobre `88f7c92`). Os três recortes `figma-1/2/3.png` (1170×2532, soltos na raiz, ignorados por `/figma-*.png`) foram conferidos em tamanho real contra dado de terceiro (FSD 21.4): são **mockups de protótipo do app do HGS em construção** feitos no Figma (grade de quartos por andar, detalhe de um quarto com ações, checklist de conferência de limpeza); os nomes visíveis ("João Silva", "Maria Silva", "João da Silva") são genéricos de teste; sem documento, telefone, e-mail, valor real, nome da pousada, nome de funcionário, credencial ou barra de navegador. Recorte de 150px no topo tirou a barra de status do simulador e o notch. Conversão para WebP q82 com Pillow (não é dependência do site): `imagens/processo/app-grade-de-quartos.webp` 75,6 KB, `imagens/processo/app-detalhe-do-quarto.webp` 79,2 KB, `imagens/processo/app-conferencia-de-limpeza.webp` 77,8 KB (todas 1170×2382, << 150 KB). `imagens/processo/.gitkeep` removido. `index.html`: nova `<section class="section" id="processo">` logo após o card do HFS — `section-label` "processo de design" + `h2` "Protótipo e design system do app" + `p.processo-intro` (linha de contexto) + `div.processo-imagens` com três `<a class="projeto-imagem" … target="_blank" rel="noopener">` + `<img width="1170" height="2382" loading="lazy" alt="…">`; bloco delimitado por comentários `FASE 6` que incluem o `<hr class="divider">` anterior (removível inteiro sem efeito colateral — FSD 12.6 / C3). Dividers: 2 → 3. CSS aditivo "FASE 6" (entre "FASE 5" e "AJUSTE DE RESPONSIVIDADE"): `.processo-intro` e `.processo-imagens` (grid `repeat(3, 1fr)`, `gap` 16px) + media queries próprias (`≤768px` duas colunas, `≤480px` uma). Imagens reaproveitam `.projeto-imagem`/`.img-overlay` da Fase 5. Nenhuma regra herdada reescrita; nada removido além do `.gitkeep`; chaves 97/97; `@media` 4. `js/script.js` **não tocado** — o seletor `.projeto-imagem` da Fase 5 já cobre os três recortes; sem JS, o `<a>` abre a imagem em nova aba. **Testes locais** (`python -m http.server`): `/`, `css/estilo.css`, `js/script.js`, `favicon.ico`, `imagens/avatar.webp`, os `.webp` de `hgs`/`hfs`/`processo`, currículo em PDF → **200**; `node --check` OK; sem `style=`/`onclick=`/caminho absoluto; `.btn-primary` **1×**, `.btn-ghost` **3×** (inalterado); `.projeto-imagem` **7×**; peso total dos assets servidos **557,1 KB** (teto 1,5 MB); maior imagem 86,7 KB (teto 150 KB); `git check-ignore` confirma as três PNGs brutas fora do versionamento. Sem erros registrados. **Ressalva:** varredura de responsividade a partir de 360px fica para a Fase 10. Aguardando conferência visual do autor no Live Server / celular; depois: push do `rascunho`, merge em `main`, push do `main`, retorno ao `rascunho`. **Próxima fase: Fase 7 — Seção de tecnologias e linha de formação.**
